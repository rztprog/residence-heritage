<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Résidence Héritage - Collocation etudiante" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="mapbox-token" content="<%= ENV['MAPBOX_KEY'] %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href='https://api.mapbox.com/mapbox-gl-js/v3.3.0/mapbox-gl.css' rel='stylesheet' />
    <script src="https://kit.fontawesome.com/c3358e0237.js" crossorigin="anonymous"></script>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/lightbox2@2/dist/css/lightbox.min.css" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/lightbox2@2/dist/js/lightbox-plus-jquery.min.js", defer: true %>
  </head>
  <body class="min-h-screen flex flex-col">
    <div class="flex-1 flex flex-col">
      <%# Navbar %>
      <%= render 'shared/navbar' %>

      <%# Root Banner %>
      <% if current_page?(root_path) %>
        <div class="h-[calc(100vh-5rem)] hidden">
          <%= cl_image_tag('banner.png',
            class: "w-full h-full object-cover") 
          %>
        </div>
      <% end %>

      <%# Content %>
      <main class="mx-auto w-full flex flex-col flex-1">
        <%= yield %>
      </main>
    </div>

    <%# Footer %>
    <%= render 'shared/footer' %>
  </body>
</html>
